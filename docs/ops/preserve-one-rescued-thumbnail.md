# Selected original thumbnail: bounded Drive preservation proposal

**Current outcome: the separately authorized singleton Drive upload, staging pointer promotion and one normal application restore all completed.** Earlier sections retain the review plan and implementation history. This is independent migration tooling; no application changes were needed.

## Pinned scope and verified destination

- Tutorial: `48790ac0-6ad1-4ce6-8ac4-7f7c26c428a6`.
- Selected English thumbnail: `70175471-10ac-4d26-81d5-da503701b942`, completed, still **not_reviewed**. Preservation must not change that verdict or selection.
- Source file: **146,123 bytes**; SHA-256 `94113309775e2b0e8eb9077711be893c0c27a6aa7bb48f068ca3b32f122b719f`; MD5 `549dc5cd3a6c891463cc2853dc556c9a`.
- Source selection/revision digest: `dc4ca9128a562b465d549c6abee5957790d4b11fae12665ae749bfb357b2e3fd`. The read-only planner hashes the exact ordered source JSON projection: thumbnail ID, subject, language, status, selected flag, verdict, output path, updated_at plus original job ID/status/channel/final path. Path and selection are therefore bound, not merely image bytes. Ancestors are non-link directories without group/other write bits; file is a single-link regular file opened O_NOFOLLOW with stable stat checks and repeated source-row comparison.
- Verified owner account: **konrad.schrein@gmail.com**.
- Proposed destination: existing folder **`1cztfSoY3vjFoHHfPQHccAGn6P797sHaL`**. Authenticated metadata confirms it is the actual parent of this original's already-preserved video, a nontrashed folder owned by that account with canAddChildren and no shared-drive ID. Quota/headroom check passed. No folder creation or inferred per-channel folder is proposed.
- Read-only plan hash: `8aedd3262e58601c5f0af01205b98cfed8f3dfefa1f380688df81bdbb5cdaad6`.

Staging has no selected-image bytes, no thumbnail storage parent and no immutable Drive version for this image. The source image exists but also lacks verified archive evidence. Copying to VPS2 alone would **not** satisfy this preservation requirement.

## Proposed execution after separate approval

1. Create a fresh **VPS2-only** private work directory (0700/root), singleton manifest version `selected-thumbnail-preservation/1` and append-only hash-chained ledger (0600). Never reuse/alter the existing five-video manifest or ledger. Manifest must contain the exact two IDs above, source canonical path/revision/stat token, SHA/MD5/size, destination account/folder and intended thumbnail identity. Acquire an exclusive ledger lock and reread its chain after acquisition. No files, settings, database rows or credentials are written on the source server.
2. Recheck source selection/revision and stable file fingerprint read-only. Relay only these 146,123 bytes over the existing strict-host-key source/VPS2 aliases, through bounded Node binary pipes/memory, to one exclusive 0600 private **VPS2 spool**. Nothing enters OneDrive or local media files. Verify spool size/SHA/MD5 and MIME by signature, then recheck source bytes and selection. This is a private upload input, not an application asset or a claim of Drive preservation. Retain it until review; no automatic source/spool deletion is proposed.
3. Use existing staged authenticated configuration, `DriveClient`, `DriveTokenProvider` and `preserveOne` state-machine ports. Recheck account, actual folder parent/permissions and quota immediately before uploading. Keep the five-video parser unchanged: use a separately validated one-thumbnail manifest. Configure kind **thumbnail**, job ID above and exact cfSourceSha256; do not use final_video. Existing `findExistingArtifact` already rejects multiple/incomplete matches. A found candidate must also pass owner, actual parent, MIME, size, MD5 and full SHA readback. Conflicting or ambiguous matches stop for review.
4. Before any remote create, fsync an intent event tied to this manifest hash, job, thumbnail ID and source SHA. Create a new resumable object named deterministically for this exact thumbnail ID, with `cfJobId`, `cfKind=thumbnail`, `cfSourceSha256`; never update an existing object's bytes. Fsync its sensitive resumable URL in the protected ledger before sending data. Use the existing bounded offset/chunk logic against the immutable private spool. Recheck source revision before upload and again before final verification. Do not store session URLs in public output or general application records.
5. On an ambiguous create/final response, record uncertain and stop. Resume only an acknowledged saved session or reconcile one exact appProperties+SHA object after independent verification. An intent without an acknowledged session cannot create another object automatically. Drive appProperties do **not** provide a cross-client uniqueness constraint: the exclusive ledger prevents this tool's concurrent attempts, while pre/post-search must detect an independent competing upload. Multiple objects mean reconciliation, not deletion or arbitrary selection.
6. Full readback of the acknowledged exact Drive ID must match all **146,123 bytes**, SHA and MD5; metadata owner/parents/version must remain stable. Recheck the source image/selection once more, then fsync a verified event. Only this establishes preservation. A copied spool, uploaded response, filename, or unverified MD5 alone does not.

## Separate staging receipt/current-pointer transaction

Prepare and rehearse before any commit mode is enabled. Reuse the reviewed full-staging-backup and protected audit primitives, but do not weaken the fixed-five-original executor to accept arbitrary scopes. A new fixed-one-thumbnail transaction should require:

1. Exact staging DB/user, service UID1000, stopped workers/scanners, a fresh protected full backup with pg_restore listing/SHA audit, and fresh authenticated exact-object readback. Pin complete current staging tutorial/thumbnail/artifact snapshots as well as the separate source selection/revision. Source data remains untouched.
2. One transaction: root-row lock, selected-thumbnail row lock, sorted current/prior media-path leases, then existing artifact-row locks/CAS. Require this exact selected original image, path, language and completed state; reject selection/revision changes, mismatched current files, symlinks, unrelated-kind ownership or unique-key conflicts. Preserve the original not_reviewed verdict. Existing local absence is acceptable; restoration is not part of pointer promotion.
3. Expected initial state is **zero** thumbnail storage parents for this original. Create one deterministic parent for tutorial_job/thumbnail/en using its unchanged exact output path, insert one immutable verified Drive version, and populate that parent's current uploaded pointer/SHA/MD5/size/receipt timestamp in the same transaction. This is **1 parent insert + 1 version insert + 1 current-pointer update** using the already-rehearsed ordering; no pending/skipped state commits separately. If an exact parent already exists on retry, require matching ownership/evidence and perform no unnecessary changes. Preserve any prior non-null Drive identity before replacement or stop on a conflicting immutable receipt.
4. Do not call source `markUploaded`, change the selected thumbnail row, promote approval, touch a final-video pointer, enqueue work or hydrate bytes during the transaction. Read-only reconciliation must verify the one current pointer/version and unchanged full tutorial/thumbnail rows. Lost COMMIT acknowledgement is unknown until exact-state reconciliation, never an automatic retry. Preserve backup plus before/after/uncertain audit files privately on VPS2.
5. Only after successful reviewed promotion would a separately authorized application `withTutorialMedia(kind:'thumbnail')` restore/open test prove that the existing UI can obtain the image from its new verified Drive pointer. Until then report **preserved remotely, not locally restored**.

## Review boundary

The read-only implementation `scripts/migration/plan-rescued-thumbnail-preservation.ts` accepts no execution flags. It performed only source SQL/filesystem reads and authenticated Drive metadata/account/quota reads; it has no upload, copy, source-write or DB-write branch. No singleton upload executor or pointer-commit mode has been implemented for this thumbnail yet. Root must review the destination, private-spool/ledger plan and exact transaction before execution tooling or writes proceed.

## Singleton upload executable prepared — awaiting review, NOT executed

The upload-only implementation is now prepared in migration tooling (no application/package changes). It has not been copied to VPS2 or executed. No pointer-commit executor has been added and the fixed-five tool remains unchanged.

- `single-thumbnail-source.ts` performs the same exact source SQL/selection fingerprint read, stable O_NOFOLLOW file read, fixed SHA/MD5/146,123-byte and MIME validation. It returns image bytes only to the local coordinator's bounded memory buffer; no source files or database records change.
- `single-thumbnail-policy.ts` pins the approved job, thumbnail, bytes, hashes, source-revision digest and destination folder.
- `upload-single-thumbnail-remote.ts` is the VPS2-host upload service. It only accepts `--execute-exact-thumbnail` and coordinated JSON-line input. It requires Linux root and protected staging directory. Its fixed private work directory is `/var/tmp/tutorial-single-thumbnail-70175471-10ac-4d26-81d5-da503701b942` (0700), with exclusive 0600 manifest/spool/ledger lock and a fsynced hash-chained receipt ledger. The same fixed directory is reused for reconciliation, not silently replaced with a clean ledger. Conflicting manifests, spools, partial ledger lines and stale locks block execution. It does not automatically clear someone else's lock.
- The service uses existing `DriveClient`/`DriveTokenProvider` and `preserveOne`, with **maxAttempts=1** to prevent an internal HTTP retry from creating another object after a lost response. Metadata/account/headroom validation precedes uploads; exact parent/owner/MIME/size/MD5, full SHA readback, stable remote version and duplicate-search checks precede verified status. It never overwrites Drive bytes or deletes files.
- Before upload, before the chunk and after readback, the service requests a fresh source check from the coordinator, validates the exact source revision/SHA/stat token, and waits for acknowledgement. Source selection changes cannot be hidden by an unchanged private spool. Only the VPS2 host performs upload writes; the source remains read-only.
- `upload-single-thumbnail-cli.ts` is the local Node coordinator. It accepts only `--execute-exact-thumbnail`; before sending private bytes it requires that the reviewed remote executable at `/opt/tutorial-recovery-staging/tools/upload-single-thumbnail-remote.mjs` has safe ownership/mode and the same SHA as the code-only sibling bundle. Binary image content travels only through captured SSH pipes and JSON/base64 memory frames, never local files/OneDrive/tool output. The runner does not install code or start a web/worker service. Missing output, disconnects, timeout or an unverified result are reported as **uncertain**, requiring private-ledger reconciliation; it does not automatically rerun.

For review, bundle both upload TS entry points as Node ESM into a local temporary code-only directory, retaining the matching filenames. Installation of that reviewed code-only remote bundle and any execution require root approval. The upload service is independent of the staging web process, so a web upgrade cannot silently restart an upload; lost SSH/process acknowledgement still requires reconciliation. Completion reports only verified counts, exact object ID/hash and private ledger location, never a resumable URL. It does not create storage parents, current pointers or approvals.

Five dedicated tests passed: fixed singleton scope, lost create/no blind retry, failed durable ledger stopping before create, changed source selection preventing verified status, and a real `DriveClient` instance with mocked transport making exactly one ambiguous create request. Both ESM bundles build. These tests do not claim a real upload, Linux spool rehearsal or Drive preservation; none has occurred yet.

## Actual authorized singleton upload — completed

After root reviewed the full executable, the reviewed code-only bundle was installed exclusively at `/opt/tutorial-recovery-staging/tools/upload-single-thumbnail-remote.mjs`, root-owned with SHA-256 `31dc9606bee2c4d667ef45abb294f3e0ecf5d2474e5d3524139c20aad7b789c5`. No differing existing code was overwritten. The coordinator was invoked **once**; no retry was needed.

The selected original thumbnail is now preserved as Drive object **`1S5yw7OnPaJ0kgjwiHJo7yn2SjVhLrXmZ`** in the verified account/folder above. Owner, actual parent, MIME, metadata MD5, full readback SHA/size, remote version stability and unique exact appProperties lookup passed. Fresh source revision/selection/stat checks also passed before and after transfer. Verified size is **146,123 bytes**, SHA-256 **94113309775e2b0e8eb9077711be893c0c27a6aa7bb48f068ca3b32f122b719f**.

Protected VPS2 ledger: `/var/tmp/tutorial-single-thumbnail-70175471-10ac-4d26-81d5-da503701b942/receipts.jsonl`. Manifest SHA-256: `b4b5585e49e2822b0bdd24a4d907184de40c3f041725ab6fcb63b0255064467c`; verified ledger head: `115b4aef9d52d0eb470eeb622b71f2d02fa8e25f0ced14a1d479f704c6d2c940`. Private manifest, immutable source-image spool and fsynced ledger remain on VPS2. Session URLs never entered public output or local files.

**Source writes0, database writes0, pointer writes0.** No selection or review verdict changed, and no source files were deleted. This establishes remote preservation, not an application pointer or a restored thumbnail in the application media tree. The singleton staging receipt/current-pointer transaction still requires separate implementation/review/authorization.

## Separately authorized staging promotion and application restore — completed

The dedicated `single-thumbnail-promotion-{plan,transaction,cli}.ts` implementation keeps the original fixed-five executor unchanged. Root narrowed this singleton to an absent parent or an exactly already-applied parent **and** immutable receipt; replacement of any different existing pointer/path is rejected. SQL and result checks require initial write counts **1/1/1**, or idempotent **0/0/0**. Tests include count-bound violations, wrong selection/verdict, changed/ambiguous pointers, stale/wrong remote proof and uncertain COMMIT acknowledgement. Fifteen dedicated singleton tests pass.

An exact directory dry-run found only `thumbnails/48790ac0-6ad1-4ce6-8ac4-7f7c26c428a6` missing under `/opt/content-forge/media`. Root separately approved creation; the existing audited `applyDirectories` created that one empty directory as UID1000/mode0700 against plan hash `340218f241921ec8a19fcdd4de38d3e8978f635bf57a8aeba86cbf944425db08`. No existing permissions or files changed. Afterward planned directories0.

The default rollback rehearsal then passed with one parent, one immutable version and one pointer update, retained writes0, full job/thumbnail state unchanged; plan hash `ddc312ff25abd797e3f7d6805cf2a0c511f0063c39498b404d81c5d9ba1d68da`.

Following root's explicit commit authorization, a fresh protected full staging backup was created and verified: `/var/tmp/tutorial-preservation-promotion-AMshbE/staging-before-promotion.dump`, **28,044,902 bytes**, SHA-256 **b3db003f9759a5c9732482cb1013f12a3c77ce73c28404b4721cfcd06263891c**, completed **2026-09-09T01:32:51.040Z**. pg_restore listing, private permissions, full SHA reread and freshness guards passed. Backup/before-commit/after-commit audit files remain protected in that directory.

The one commit succeeded: **1 parent insert + 1 version insert + 1 current-pointer update**, 3 committed writes, no source/media writes. Independent post-read state was **exact_applied**, with unchanged full tutorial row, thumbnail selection and **not_reviewed** verdict. Commit plan hash: **5f26329a9fe2aa9589e270a39c8f70e13b3ec22d9aeedd9c814ed0467e766c4e**.

Root also separately authorized one application restore. `restore-rescued-thumbnail-via-application.ts --restore-exact-thumbnail` used deployed `/app/packages/storage/dist/tutorial-media.js` `withTutorialMedia(kind:'thumbnail')`, without downloader/lease overrides. Its same-transaction root/thumbnail locks and normal media lease retained existing SHA/no-clobber/symlink guards. The exact selected image became present at its original application path: **146,123 bytes**, SHA-256 **94113309775e2b0e8eb9077711be893c0c27a6aa7bb48f068ca3b32f122b719f**. Full tutorial/thumbnail/artifact rows were unchanged by restore. No approval, generation, schedule, source deletion or other restoration occurred.
