# Append-only import of the five preserved English originals

Current outcome: the explicitly authorized staging-only promotion completed on 2026-09-09; see the final outcome section below. Five verified objects total 58,224,531 bytes. Earlier sections retain the plan and rehearsal decision history. The protected source ledger is `/var/tmp/tutorial-migration-preserve-q09ypb/receipts.jsonl`. Keep that ledger and its manifest on protected servers: session events contain sensitive resumable URLs. Never print them or copy them into this repository/OneDrive.

## Phase A: preserve immutable evidence only

1. Freeze the completed ledger for reading (no active `.lock` or preservation writer). Record independent SHA-256 checksums of the exact manifest and complete newline-terminated ledger. Transfer them only over the already verified server path into a new 0700 staging temp directory, files 0600, no symlinks/hardlinks. Protect the full ledger; do not include session URLs in database payloads or reports.
2. Validate manifest membership is exactly the five approved IDs. Validate every ledger event's sequence, previous hash, manifest hash, event timestamp and allowed job ID. Require each job's **last event** to be verified, with exact manifest SHA/size and Drive object ID. A later uncertain event blocks import even if an earlier verification exists.
3. Resolve staging ownership with an exact query of those five IDs only. Require one `storage_artifacts` row per job with `owner_kind='tutorial_job'`, `kind='final_video'`. The unique constraint does not include owner_kind: reject a collision belonging to another format. Missing artifact mappings are blockers; this version-only importer must not create runnable fake jobs or pending upload records.
4. Link each manifest sourceRevision to its immutable source tutorial archive. The preservation digest covers the ordered tuple `id,status,final_path,completed_at,recording_path,audio_path,recorded_at,md5(script_text),source_job_id,parent_job_id`, encoded exactly as source PostgreSQL JSON returned it. Preserve NULLs and timestamp precision/format. Do not recompute from a transformed UI DTO or rounded JavaScript Date and interpret a mismatch as data loss. If the older migration snapshot differs, capture a new protected read-only source projection and reconcile; never overwrite the original archive. Current staging status may intentionally differ and is not historical evidence.
5. Run `scripts/migration/plan-preservation-receipts.ts` against these verified identities. Its result is private: inserted rows contain paths and Drive IDs. Public output must be counts/bytes/checksums only. The pure helper has no I/O, DB or provider effects and does not itself authenticate a server or make a hash chain tamper-proof; trusted transport and independently recorded input hashes remain mandatory.
6. On the explicitly named staging database only, begin a transaction, take a migration advisory lock, lock the five parent artifact rows in stable UUID order, and recheck ownership/IDs. Check all existing `(artifact_id,drive_file_id)` versions. Matching path/SHA/size/MD5 plus a non-null verified timestamp is idempotent. A differing or previously unverified immutable row is a conflict, not permission to UPDATE or silently ignore.
7. Insert only `artifact_id, drive_file_id, vps_path, bytes, checksum_sha256, drive_md5, verified_at`. Use the verified ledger event's `at`, not import time. Leave recorded_at to insertion time. Use parameterized SQL and `ON CONFLICT DO NOTHING`, then reselect and assert exact expected evidence before commit. Database append-only guards remain enabled. Default rehearsal rolls back. Explicit commit separately requires five expected rows and 58,224,531 total bytes; a mismatch aborts.
8. Retain a protected import audit alongside the original files containing target DB identity, input checksums, ledger head, archive source identities, insert/existing counts and commit outcome. The current versions schema has no sourceRevision/manifest-hash column, so do not pretend it stores that full provenance. Do not edit the frozen application image/schema for this operation.

## What this does NOT claim

Versions-only import does not update `storage_artifacts`, tutorial statuses, review/publication approval, schedule, upload dispatch or daily upload accounting. It will not currently make a Drive-ready badge appear or enable on-demand restore: `packages/storage/src/tutorial-media.ts` reads the **current** artifact pointer, not the versions table. Do not call `markUploaded`, which changes the current pointer/state and assigns a new verified_at.

The reported independent restore of 13,841,434 bytes with SHA-256 `ea3548ae13bde41c583677a651d554451eabb3a507bebdf87691117d750eaa34` proves that exact object's restore only. It is not evidence that all five files exist locally on VPS2. No import should create local-presence flags for the other four.

## Separate optional Phase B: make exact current revisions retrievable

Only after an explicit current-pointer decision: compare the current staging tutorial's final path, source revision and artifact identity against the preserved revision; verify the exact remote ID still belongs to the expected Drive account, is not trashed and matches size/MD5 (full SHA readback if verification freshness requires it). Resolve actual remote parents by ID: the preservation manifest folder is a requested destination, not proof of the parent of a reused existing object. Under root-row then sorted media leases and artifact-row/CAS protection, archive the previous pointer before any replacement. Set a current pointer only for an exact matching revision; otherwise retain historical evidence only. Never overwrite a different local file, fabricate approval, enable workers, or delete source bytes as part of receipt import.

## Actual read-only preflight

`scripts/migration/preflight-preservation-receipts-cli.ts` is a fixed-target, no-argument SSH preflight using the existing verified `cf-vps-deploy` and `vps2` aliases. It captures private manifest, ledger and narrowly selected DB results in memory only; it outputs aggregate counts/checksums. It does not print raw rows, resumable URLs or errors containing private values. Bundle it as Node ESM outside OneDrive; the bundle contains code only. No application container is started.

Observed staging database `tutorial_staging_cf_20260908`: five source revisions match both `content-forge-main` immutable source archives and all five current runtime jobs. Only two have existing unique tutorial/final-video artifact rows; three current runtime jobs lack those parent records. Thus versions-only import is blocked, not completed. Missing storage rows are not missing tutorials or evidence of missing Drive bytes. Creating three parent records is a separate reviewed mutation; the read-only CLI deliberately does not invent them. A blocked preflight emits a stable preflightHash and null planHash rather than pretending to have an executable insert plan.

## Parent preparation: reviewed rollback rehearsal only

`scripts/migration/prepare-preservation-parents.ts` prepares the three missing parent identities from the five existing runtime originals and exact archived revisions. Proposed new rows use deterministic UUIDs, the current channel/language and exact final path, **state=skipped**, `error_kind=preservation_history_only`, and a descriptive explanation. Current Drive ID, verified_at, bytes and checksum remain NULL. Existing parent rows are not updated. No tutorial rows are invented or changed. Five verified versions attach to the resulting parent identities.

`scripts/migration/rehearse-preservation-parents-cli.ts --rehearse` is the only accepted invocation. It connects only to the fixed isolated staging DB, takes an advisory lock and stable original/parent row locks, rechecks exact runtime fences and ownership, tests the three parent inserts and five immutable receipt inserts, then unconditionally **ROLLBACKs**. It has no commit switch. Private JSON is hex encoded for SQL transport and never logged; process errors are suppressed. The existing append-only trigger remains enabled. The imported source revision's script is used only in private in-memory fence comparison, never the public summary.

Actual rehearsal passed: **3 proposed parents, 5 proposed immutable versions, 58,224,531 verified bytes, zero committed writes/current-pointer changes**. Plan hash `2312e0aede5e44f9c75e4df25d954ccc22c7b699cf08b76ef5cbfec1f3b2ffe5`. Independent post-rehearsal preflight still finds exactly three missing parents, confirming rollback. Twelve planner tests pass. No conflict was detected in the actual rehearsal.

This is not authorization or a ready-made switch to commit. Any commit implementation requires separate root review, private backup, fresh fences and post-commit verification. `skipped` records deliberately avoid claiming an upload/present file; they are not a blanket permanent scanner exclusion. Starting any scanner or promoting current pointers remains a separate operation. Until then the UI/restore behavior is unchanged.

## Phase B bounded proposal (not executed)

`plan-preservation-promotion.ts` is a pure proposal builder, not an execution CLI. It combines parent preparation and the five receipt identities with fresh, independently acquired remote proofs and complete current artifact snapshots. The currently observed five matching runtime revisions and three absent parent rows establish scope, not permission to skip fresh transaction fences. Remote proof inputs must come from the authenticated Drive adapter, not operator-provided success booleans: exact owner, ownedByMe, not trashed, size, MD5, SHA-256 and actual parent IDs. The pure planner requires a proof timestamp no older than 15 minutes; it does not itself contact Drive or prove the authenticity of those inputs.

Recommended minimal implementation uses one short staging-only SQL transaction, with provider calls completed **before** locks:

1. Keep every storage scanner, upload worker, cleanup/retention worker and generation writer stopped. Check that no active/uncertain upload session exists on the two current parents; an `uploading` state or resumable URI blocks promotion. Existing web reads may remain available because they use the same media lease. Take a new private DB backup. Read all current artifact columns and obtain remote proof using the protected receipt IDs. Recheck safe local targets: missing is allowed only with an existing safe parent directory; existing bytes must exactly match SHA/size. Never copy, delete or overwrite a local file.
2. In the fixed staging DB, begin with bounded lock/statement timeouts and the migration advisory lock. Lock all five tutorial root rows in sorted UUID order, then acquire sorted transaction advisory locks using the exact application key `hashtextextended('tutorial-media:' || path, 0)`, then lock existing artifact rows in sorted UUID order. Recheck every source revision/fence, ownership, uniqueness and the full expected artifact snapshot using JSONB equality (not merely ID or updated_at). Recheck local stable file identity/hash under the lease. A raw external filesystem writer does not honor database leases, hence quiescing writers is a prerequisite, not optional.
3. Insert the three deterministic parents only if still absent. Preserve every previous non-null Drive identity in `storage_artifact_versions` before replacing it, including unknown/null verification fields without upgrading them. Compare any existing immutable conflict to the prior snapshot: a differing row aborts, never update it. Insert the five incoming verified versions with their original receipt timestamps; reselect and compare all evidence after `ON CONFLICT DO NOTHING`. If a prior pointer shares the incoming ID but contradicts the immutable evidence, stop for reconciliation rather than silently replacing history.
4. In that **same transaction**, CAS-update all five current rows to the exact preserved path, ID, bytes, SHA, MD5 and `state=uploaded`, with verified/uploaded timestamps from the receipt, exact bytes_uploaded, cleared stale error/resumable fields and current updated_at. Use actual remote parent ID only when uniquely known, leave folder_path NULL rather than borrowing a requested folder name. Do not modify job status, approval, schedule or uploader records. Require five matching updates and 58,224,531 verified bytes before the default ROLLBACK; any future explicit commit requires separate review. Do not first commit three skipped parents and leave a later scanner to finish migration.
5. Verify the committed state separately if commit is later approved. History preserves the old pointer; current uploaded rows are eligible for exact on-demand restoration and excluded from the ordinary uploaded/skipped scanner candidate selection. This proves Drive retrievability, **not local presence**. An actual restore remains a separate requested action.

The existing `markUploaded` helper is unsuitable for this migration: it opens a separate transaction and assigns a new verification time, without the five-root/media/CAS fence. The existing materializer already supplies bounded exclusive temporary files, SHA/size verification, fsync and no-clobber hard-link installation; do not weaken its missing-parent, symlink or mismatch guards. No application changes are required for a correctly populated current pointer.

The pure proposal retains complete prior snapshots, sorted root/media lock requests and explicit `execute=false`, `mediaWrites=0`, `jobWrites=0`, `scheduleWrites=0`. Its payload is private, not suitable for stdout. It is deliberately **not yet a real five-object remote verification or transactional promotion rehearsal**; the executor and authenticated fresh proof collection require root review first.

### Rollback-only Phase B executor

`rehearse-preservation-promotion-cli.ts` accepts no arguments or `--rehearse`; every other flag, including commit flags, fails closed. Bundle with esbuild as Node ESM outside OneDrive. Code is sent through existing strict-host-key SSH stdin: no source files or credentials are copied locally. The source-side proof collector reads existing protected OAuth configuration, checks the expected account, reads each exact Drive object fully through memory, independently hashes SHA-256/MD5 and bounded bytes, then rechecks metadata version/ownership/parents. Tokens and media never appear in output or files.

Before proof collection and again before the transaction, the runner checks the exact three staging Compose services, exact custom web startup command and process roles (one main node/tsx server and at most one esbuild service child), plus the four disabled activation flags from the guarded deployment helper. There is no new service launch or scanner activation. Startup source audit found only Next and pg-listen initialization; the pg-listen timer reconnects its listener, not a background storage writer. This is a bounded inventory check, not protection against an administrator starting a new worker concurrently; deployment and other writers must remain held throughout rehearsal.

The existing UID1000 web container supplies its PostgreSQL driver and safe local-file access. The transaction implementation acquires the application's exact sorted media advisory-lock keys after original-row locks, compares full artifact rows, rehashes existing local files with O_NOFOLLOW and stable stat tokens while locks remain held, then rehearses parent/history/pointer writes. Missing parent directories or differing bytes stop before any transaction. A finally block explicitly ROLLBACKs on success and failure; no commit option exists. A separate read-only query verifies all parent rows and immutable versions remain unchanged afterward. Public output contains only counts, a plan hash, and a bounded failing stage; no private row details.

Unit tests cover rollback on success, SQL failure and local-file snapshot change, lock order and the absence of a commit statement. Both incoming and differing prior current media paths are validated against the canonical allowed root and included in sorted leases. Twenty-three preservation tests pass.

Actual candidate9 attempt passed the strict service/process/activation inventory and fresh authenticated full readback of **all five Drive objects, 58,224,531 bytes**. It stopped before the transaction because **three parent directories are missing**, with **zero local-file conflicts**. These manifest paths were absent from the earlier directory skeleton, which was derived only from existing storage artifact rows. No database writes, directory creation or media restore occurred. Prepare only these exact missing directories through a separately reviewed directory-only plan, then refresh proofs and repeat the rollback rehearsal. The transactional promotion itself has not yet been rehearsed on candidate9.

### Follow-up: reviewed directories and successful rollback

Root separately approved exactly these empty relative directories under `/opt/content-forge/media`: `tutorial/2071be5e-9956-42be-9803-04b7d418848b`, `tutorial/409f215a-c13d-4937-9dee-3c6aafd995a2`, `tutorial/48790ac0-6ad1-4ce6-8ac4-7f7c26c428a6`. The exact-five wrapper reused `applyDirectories`, required the reviewed fresh plan hash `fa7024e09520cdf1aa43cbff44c0e92809bfe0f094b732dfda07a400cd5f2909`, created only three directories, and verified UID1000/mode0700. An independent dry-run showed five safe paths, six existing directories, zero planned/blocked; after hash `af49416ab287ca7295d8a0f91e92e676411b690292e6c55349776f4c03940888`. No existing directory permissions or media files changed.

The operator PC clock was observed approximately 24 seconds behind the source/VPS clocks. Initial proof freshness therefore stopped safely before SQL. The runner now obtains authoritative current time from staging PostgreSQL for planning, matching the SQL fence's clock. It does not loosen the strict no-future/15-minute proof policy or rewrite receipt timestamps.

After fresh proof collection, the **actual candidate9 rollback transaction passed**: three parents, five immutable receipt rows and five exact current pointers rehearsed, 58,224,531 remote-verified bytes, zero committed DB/media writes. All five local target files are absent; this result proves remote bytes and the transaction protocol, not local restoration. Neither of the two existing parents had a non-null prior Drive pointer. Full current rows, artifact mappings and immutable versions were independently unchanged after rollback. Plan hash `e9c9e2d7d53e4467e231c03f3cea5bd5c0314118ec9f7b72af8578ca262223e0`. Only the separately approved three empty directories persist. **No commit option has been implemented or authorized.**

### Proposed explicit commit mode — implementation prepared, NOT executed

Following review of the successful rollback, root authorized preparation of a commit implementation but **has not authorized running it yet**. This section supersedes the earlier implementation-status note, not the historical zero-commit result. The CLI still defaults to rollback; only the exact `--commit-five-verified-originals` argument requests promotion. There are no arbitrary database, owner, job, source-path or scope arguments.

Precise additional behavior:

- The transaction independently requires the five fixed rescued job IDs in both update and original-row fence lists. It retains staging DB/user, UID1000, append-only trigger, full current-row CAS, strict source revision and fresh remote proof guards, sorted prior/incoming media leases and local stable-byte checks. It changes only three absent storage parents, five immutable versions and five exact current pointers. It does not update jobs, approvals, schedules, media or source systems.
- Commit mode requires a new **full** custom-format staging DB backup on VPS2 before transaction dispatch. `createPromotionBackup` verifies the exact DB/user, requires 2 GiB available under `/var/tmp`, creates a random `/var/tmp/tutorial-preservation-promotion-XXXXXX` directory mode0700/root and `staging-before-promotion.dump` mode0600/exclusive, streams `docker exec tutorial-recovery-staging-postgres-1 pg_dump -U tutorial_staging -d tutorial_staging_cf_20260908 -Fc` directly into its remote file descriptor, and fsyncs it. No dump bytes pass through the operator PC.
- It verifies a nonempty regular single-link protected dump, runs `pg_restore --list` via that same staging DB container with the dump as stdin, requires TABLE DATA entries for users, tutorial jobs, current artifacts and immutable versions, hashes the dump SHA-256, and writes/fsyncs a protected `backup-audit.json` with DB identity, size/hash, listing result, authoritative server timestamp, plan/manifest/ledger hashes and intended counts. Failed backups are retained privately, not reused or deleted. This listing validation is not a full restore rehearsal and is not described as one.
- Before dispatch, `verifyPromotionBackup` independently rechecks private permissions, file identity/size, full SHA and matching audit. The transaction separately requires its verified backup attestation to be no older than 15 minutes and not future relative to PostgreSQL time. It repeats fresh remote/source/local checks as before; no TTL is relaxed to accommodate the backup.
- `before-commit.json` is written exclusively and fsynced in that protected directory **before** sending the transaction. It records only plan hash, intended counts and dispatch intent. The sole SQL COMMIT occurs after all assertions. The executor marks the commit attempt before issuing COMMIT. A lost acknowledgement never becomes a claimed rollback or zero writes: it returns uncertain/null writes, performs read-only reconciliation of exact current pointers, five versions and unchanged runtime rows, and writes `reconciliation.json` when possible. There is no blind retry.
- Normal acknowledgement is followed by independent read-only verification and protected `after-commit.json`. Exactly applied retries create no new parents/versions and skip current-pointer updates when desired fields already match, preserving updated_at. Timestamp-format equivalence is compared as timestamps, not raw strings; differing bytes, identities or verification evidence still fail closed. Append-only conflicts are never repaired in place.

Review files: `preservation-promotion-backup.ts` (backup and audit), `preservation-promotion-transaction.ts` (narrow commit gate/counts/uncertainty), `rehearse-preservation-promotion-cli.ts` (mode/orchestration), `reconcile-preservation-promotion.ts` (read-only state classification), and planner timestamp normalization. Twenty-nine unit tests cover default rollback, explicit backup requirement, mock-only commit, lost acknowledgment, conflicting/unchanged/applied reconciliation and already-applied replanning. **No commit invocation, backup creation or retained database mutation has been performed by this new implementation.**

The expanded implementation's **default rollback mode was subsequently verified against actual staging**: 3 parent inserts, 5 version inserts, 5 pointer updates, `commitPerformed=false`, retained writes0 and independent post-rollback rows unchanged. Plan hash `0b29cc39faf88d36664de026f982a008cafc8e5625413e8424ce7f9ad9915dd3`. An initial rehearsal caught a PL/pgSQL alias ambiguity in the new idempotence check; the alias was made explicit and the complete rollback rerun passed. This verifies SQL execution, not the still-unexecuted backup/commit branch.

## Actual authorized staging promotion — completed

Following explicit root review and authorization, `--commit-five-verified-originals` completed once against **tutorial_staging_cf_20260908 only**. The transaction inserted **3 missing storage parents**, **5 immutable verified versions**, and updated **5 current Drive pointers**: 13 committed writes. Fresh authenticated full Drive readback verified all five files, totaling **58,224,531 bytes**. The independent post-commit query verified every intended pointer field, all five immutable receipts and unchanged complete runtime tutorial rows. No job, approval, schedule, source, provider-generation or media writes occurred; no workers started. All five target media files remain absent locally, and no restoration is claimed.

Protected full backup (VPS2 only): `/var/tmp/tutorial-preservation-promotion-pb6vnv/staging-before-promotion.dump`, **28,043,042 bytes**, SHA-256 `765f3284f0757148fba01dc3a1dde0d657105e6f85987045febbb3544e7c25db`. Backup completed at **2026-09-09T00:57:28.114Z**; `pg_restore --list`, required data-table coverage, fresh full SHA reread and protected permissions passed before commit. The same 0700 root-owned directory retains 0600 `backup-audit.json`, `before-commit.json` and `after-commit.json`; nothing was copied into the local workspace. The acknowledged commit plan hash is `339572f645ef53b6d14a41381913be4da33025c0bbd6a760655dca7e8656e1f4`.

A subsequent, separately invoked **read-only** receipt preflight confirmed: `ready=true`, five archive matches, five current revision matches, **alreadyRecorded=5**, **insertCount=0**, database writes0. Its plan hash is `b235ad8f9374fc72d33e89020d0b95c3d4038ca8d2ca95e3e1602f4d3e6011e9`. This was verification, not a second commit or retry. Existing materialization can now use these exact current Drive identities when separately requested; this migration did not download or overwrite any media.

## Separately authorized application-pointer restore pilot — completed

After promotion, root explicitly authorized on-demand restoration of original `48790ac0-6ad1-4ce6-8ac4-7f7c26c428a6` only. `restore-rescued-original-via-application.ts --restore-exact-one` invoked the **deployed** `/app/packages/storage/dist/tutorial-media.js` `withTutorialMedia` helper, without an injected downloader or lease. The helper resolved the missing bytes through the current artifact pointer, effective DB/credential configuration and existing exact-revision materializer. This tests application integration, not the earlier standalone temporary Drive download.

The root-locked transaction reused the same pinned transaction for the shared media lease, verified the expected current uploaded ID/path/SHA/size, and supplied exact expected content. Existing symlink, missing-parent, exclusive partial, SHA/size and no-clobber installation guards were unchanged. Fingerprinting inside the active lease confirmed a newly present **13,841,434-byte** file with SHA-256 **ea3548ae13bde41c583677a651d554451eabb3a507bebdf87691117d750eaa34**. Full tutorial and current artifact rows were unchanged before/after. No approval, schedule, source or other media changes were performed; no broad restore occurred. The other four rescued targets were not restored by this pilot.
