# Docker Compose for Hub Web Local Development
# Includes PostgreSQL, Redis, and Hub Web app

version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: hub-web-postgres
    environment:
      POSTGRES_DB: content_forge
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: hub-web-redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5

  hub-web:
    build:
      context: ../..
      dockerfile: apps/hub-web/Dockerfile
    container_name: hub-web-app
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/content_forge
      REDIS_URL: redis://redis:6379
      JWT_SECRET: dev-secret-key-change-in-production-32chars
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
