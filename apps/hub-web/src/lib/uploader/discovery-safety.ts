/** Discovery is not an atomic claim and cannot authorize external side effects. */
export const UPLOADER_DISCOVERY_HOLD = "Inspection only: this legacy endpoint does not bind approval revisions or atomically admit dispatches. Use the guarded delivery contract; do not start uploads from this manifest.";
